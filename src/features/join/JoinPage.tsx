/** Matches padel_wireframe.html screen 3. Stub — see README "What's built vs next". */
export default function JoinPage() {
  return (
    <div className="mx-auto max-w-sm min-h-screen bg-white px-4 py-8">
      <h1 className="text-xl font-extrabold mb-4">Join a Session</h1>
      <p className="text-sm text-slate-500">TODO: 6-digit code entry + QR scan, wired to a session lookup.</p>
    </div>
  );
}
