/** Matches padel_wireframe.html screen 13. Stub — see README "What's built vs next". */
export default function FinalSummaryPage() {
  return (
    <div className="mx-auto max-w-sm min-h-screen bg-white px-4 py-8">
      <h1 className="text-xl font-extrabold mb-4">🏆 Session Complete</h1>
      <p className="text-sm text-slate-500">TODO: final standings via src/lib/scoring/standings.ts + full history.</p>
    </div>
  );
}
