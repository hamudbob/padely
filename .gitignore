node_modules
dist
.env.local
.env
*.local
.DS_Store
*.tsbuildinfo
vite.config.js
vite.config.d.ts
_to_delete/
_to_delete/
_git_stale_locks/

# Scratch dirs used when git tooling cannot unlink on this mount
_gitjunk/
_to_delete/

# E2E (Playwright). .env.test holds two real Supabase accounts' credentials,
# and tests/.auth holds their live access tokens — neither ever gets committed.
.env.test
tests/.auth/
test-results/
playwright-report/

# Database dumps: they contain every user's email address.
backups/
